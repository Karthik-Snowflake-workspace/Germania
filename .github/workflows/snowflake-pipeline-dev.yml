name: "Snowflake Component: Progression Target - dev"
on:
  workflow_call:
    inputs:
      action_type:
        required: true
        type: string

permissions:
  id-token: write
  contents: read

env:
  SNOWFLAKE_ACCOUNT: "KIPI-KIPI_PRIMARY"
  SNOWFLAKE_USER: "github_germania_dev_user"
  SNOWFLAKE_ROLE: "PSEUDO_ACCOUNTADMIN"
  SNOWFLAKE_WAREHOUSE: "github_cicd_demo_wh"
  SNOWFLAKE_CLI_FEATURES_ENABLE_SNOWFLAKE_PROJECTS: "true"
  SNOWFLAKE_DATABASE: "GERMANIA_DEVOPS_DB_DEV"
  SNOWFLAKE_SCHEMA: "CLAIMS"

jobs:
  dev-environment-execution:
    runs-on: ubuntu-latest
    environment: dev 
    steps:
      - uses: actions/checkout@v4

      - if: inputs.action_type == 'plan'
        name: "DCM Plan — Dry-run against DEV target only"
        uses: ./.github/workflows/modules/action-dcm-engine
        with:
          dcm_dir: "dcm_automation"
          dcm_target: "GERMANIA_DCM_DEV"
          action_type: "plan"

      - if: inputs.action_type == 'deploy'
        name: "DCM Deploy — Apply changes to DEV target only"
        uses: ./.github/workflows/modules/action-dcm-engine
        with:
          dcm_dir: "dcm_automation"
          dcm_target: "GERMANIA_DCM_DEV"
          action_type: "deploy"
